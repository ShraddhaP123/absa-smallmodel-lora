# Local / CPU-only (Sessions 1, 2, 7)
datasets>=2.19
pyyaml>=6.0
pandas>=2.2
scikit-learn>=1.4
pytest>=8.0
python-dotenv>=1.0
openai>=1.30          # for API-baseline variants (Session 2)
anthropic>=0.30       # for API-baseline variants (Session 2)

# GPU (Sessions 3-6, install on Lightning/Kaggle, not local)
# torch>=2.3
# transformers>=4.42
# peft>=0.11
# accelerate>=0.31
# bitsandbytes>=0.43
# vllm>=0.5
